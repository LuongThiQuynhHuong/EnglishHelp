# Network code generation

Choose naming, layering, and dependency conventions after the first network implementation establishes a stack. For new interfaces, make request and response shapes explicit, handle malformed input and failed connections, and keep project-specific credentials out of source files. Record adopted conventions here.
